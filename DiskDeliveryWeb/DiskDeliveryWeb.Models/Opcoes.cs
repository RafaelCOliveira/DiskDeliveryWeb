namespace DiskDeliveryWeb.Models
{
    public class Opcoes
    {
        /* 
        CREATE TABLE OPCOES (
        ID_OPCOES    INTEGER NOT NULL,
        DESC_OPCOES  VARCHAR(40) NOT NULL,
        FORM         VARCHAR(30) NOT NULL,
        OPC_MENU     CHAR(3) NOT NULL
        ) ;
        */
    }
}